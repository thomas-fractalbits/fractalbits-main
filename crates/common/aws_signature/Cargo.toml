[package]
name = "aws_signature"
version = "0.1.0"
edition = "2024"

[features]
testutils = []

[dependencies]
chrono = { version = "0.4", features = ["serde"] }
hmac = "0.12"
sha2 = "0.10"
hex = "0.4"
base64 = "0.22"

[dev-dependencies]
tokio = { version = "1", features = ["full"] }
